pragma solidity >=0.5.16;

// declaring our contract 
contract Election {
    //Model a candidate 
    struct Candidate {
        uint id; 
        string name; ;
        uint voteCount;
    }

    //store candidates
    
    //fetch candidates 

    constructor() public{
        candidate = "Candidate 1";
    }
}